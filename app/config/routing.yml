front:
    resource: "@FrontBundle/Controller/"
    type:     annotation
    prefix:   /

security_logout:
    path: /logout

api:
    resource: '.'
    type: 'api_platform'
    prefix: /api

app:
    resource: '@AppBundle/Action/'
    type: 'annotation'
